spring:
  profiles:
     active:
        -dev
---
spring:
    profiles:  dev  #开发环境
    application:
         name:  servicecloud-config-atguigu-dev
---
spring:
    profiles:  test  #测试环境
    application:
         name:  servicecloud-config-atguigu-test
#必须保存为UTF-8的格式，否则上传有乱码